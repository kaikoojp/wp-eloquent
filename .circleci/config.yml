---

version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: Build Docker Image
          command: make compose

      - run:
          name: Preparations
          command: make vendor

      - run:
          name: Install WordPress
          command: make setup

      - run:
          name: Test Run
          command: composer run test